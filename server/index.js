const express = require("express");
const path = require("path");
const cors = require("cors");
const port = process.env.PORT || 3000;


//Write your Express server here and make it listen for requests on the given port!